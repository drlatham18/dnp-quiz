{
  "name": "nursing-learning-companion",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "test": "node validate.js && node test-app.cjs && node test-curriculum.cjs",
    "build": "node tools/build.cjs",
    "sync:mobile": "npm run build && cap sync",
    "check:release": "node tools/check-release.cjs"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.2",
    "@capacitor/android": "^8.4.2",
    "@capacitor/ios": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2"
  }
}
